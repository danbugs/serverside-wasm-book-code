# `hello_world_python_host`

## How to run?

```bash
# install deps from requirements.txt
py -m pip install -r requirements.txt

# run the script
py main.py
# note: need greet.wasm in the same directory as main.py
```

## Toolchain versions

```bash
Python 3.12.1
```